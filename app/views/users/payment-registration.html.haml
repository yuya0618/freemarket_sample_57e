%header
  =render "shared/header"

.container
  .main
    .main__left
      = render 'shared/main-side'

    .main__right
      = render "shared/payment-registration"
        

= render partial: 'shared/bottom-banner'

/ フッターリンク （リンク貼ってません！）
= render partial: 'shared/footer-black'

= render "shared/sell-btn" 
