%header
  =render "shared/header"

.container
  .main
    .main__left
      = render 'shared/main-side'

    .main__right
      = render "partial/payment-registration"
        

.bottom__banner
  = render partial: 'partial/bottom-banner'

/ フッターリンク （リンク貼ってません！）
%footer
  = render partial: 'partial/footer-black'

= render "shared/sell-btn" 
